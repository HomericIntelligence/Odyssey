# DEPRECATED HISTORICAL ARTIFACT — DO NOT RENAME TO .mojo.
#
# This file is preserved with a `.mojo.bug` suffix so the Mojo toolchain
# ignores it. It documents the pre-#6187-fix VGG16 end-to-end test that
# triggered heap corruption (libKGENCompilerRTShared.so) under high test
# load on Mojo 0.26.1.
#
# The original restored bytes contained UNRESOLVED git merge conflict
# markers from commit 94996312 (the split-into-part1/part2 fix); the raw
# pre-resolution snapshot lives at
# `repro/bug_repro_vgg16_e2e_part1_pre_fix.merge-conflict-snapshot`
# (kept verbatim for the blog post / ADR-009 narrative).
#
# Symbols referenced in the snapshot (`ExTensor`, `tests.shared.conftest`,
# ADR-009 ≤10-test cap) no longer match the current package layout:
#   - tensor type was renamed AnyTensor/Tensor
#   - ADR-009 cap was lifted after the #6187 fix
#   - the equivalent ACTIVE end-to-end test is now
#     tests/models/test_vgg16_e2e.mojo (no longer split)
#
# Status as of 2026-05-26: NOT migrated to Mojo 1.0 syntax. The file
# stands as a documentation reference. Do not attempt to compile it.
