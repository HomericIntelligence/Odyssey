
Implement GitHub issue #1538.

**Working Directory:** /home/mvillmow/Odysseus/research/ProjectOdyssey/.worktrees/issue-1538
**Branch:** 1538-auto-impl

**Issue Title:** [Impl] Implement test stub TODOs from Issue #48

**Issue Description:**
## Overview

This issue tracks the implementation of all TODO items in the test files created in Issue #48. These TODOs represent placeholder code that needs actual implementation once the shared library components from Issue #49 are available.

## Related Issues

- Depends on: #49 (shared library implementation must be completed first)
- Related to: #48 (test suite design and stubs)

## TODO Categories

### 1. Test Implementation TODOs (tests/shared/)

**Core Tests** (~16 TODOs):
- Linear layer tests (initialization, forward, backward)
- Conv2D layer tests (shapes, stride, padding)  
- Activation tests
- Pooling tests
- Property-based tests

**Training Tests** (~20 TODOs):
- SGD tests (update, momentum, weight decay, nesterov)
- Adam tests (update, bias correction)
- AdamW tests
- RMSprop tests
- Optimizer property tests
- Numerical accuracy tests (PyTorch comparison)

**Integration Tests** (~7 TODOs):
- Basic training workflow
- Training with validation
- Training with callbacks
- Multi-epoch convergence
- Gradient flow tests

**Benchmark TODOs** (~5 TODOs):
- SGD performance benchmarks
- Adam memory benchmarks
- Optimizer comparison
- SIMD vectorization tests

### 2. Test Fixture TODOs (tests/shared/conftest.mojo)

- Tensor fixture methods (~1 TODO)
- Model fixture methods (~1 TODO)  
- Dataset fixture methods (~1 TODO)
- Timing utilities (~2 TODOs)

### 3. Coverage Script TODO (scripts/check_coverage.py)

- Implement actual coverage parsing when Mojo coverage format is known (~1 TODO)

## Implementation Strategy

### Phase 1: After Core Components (Issue #49 - Core Module)
- Implement Tensor, Linear, Conv2D tests
- Implement basic fixture methods
- Implement activation tests

### Phase 2: After Training Components (Issue #49 - Training Module)  
- Implement SGD, Adam, AdamW, RMSprop tests
- Implement optimizer property tests
- Implement training workflow tests

### Phase 3: After Data Components (Issue #49 - Data Module)
- Implement dataset fixture methods
- Implement data pipeline tests

### Phase 4: Performance Validation
- Implement benchmark tests
- Implement numerical accuracy tests
- Implement timing utilities

### Phase 5: Tooling
- Implement coverage parsing script (when Mojo coverage format known)

## Success Criteria

- [ ] All test TODO comments removed or implemented
- [ ] All tests pass with actual implementations
- [ ] Coverage meets ≥90% target
- [ ] Benchmarks validate performance targets
- [ ] Numerical accuracy tests pass (within tolerance of PyTorch)

## Notes

- These TODOs are intentional - tests were written following TDD principles
- Tests define the API contracts that Issue #49 implementation must follow
- Each TODO represents a test that will validate the implementation
- Priority should follow the dependency order: Core → Training → Data → Utils

## Estimated Timeline

- Phase 1: 2-3 days (after core components)
- Phase 2: 2-3 days (after training components)
- Phase 3: 1-2 days (after data components)
- Phase 4: 2-3 days (performance validation)
- Phase 5: 1 day (coverage tooling)

**Total**: ~8-12 days (distributed across Issue #49 milestones)

---

**Implementation Context:**
- Run `gh issue view 1538 --comments` to read the full plan and any comments
- Follow the project's Python conventions and type hint all function signatures

**Critical Requirements:**
1. Read the issue description and any existing plan carefully
2. Follow existing code patterns in hephaestus/
3. Write tests in tests/ using pytest
4. Run tests with: pixi run python -m pytest tests/ -v
5. Ensure all tests pass before finishing
6. Follow the code quality guidelines in CLAUDE.md

**Testing:**
- Write unit tests for new functionality
- Ensure existing tests still pass
- Use pytest fixtures and parametrize where appropriate

**Code Quality:**
- Type hint all function signatures
- Write docstrings for public APIs
- Follow PEP 8 style guidelines
- Keep solutions simple and focused

**File Handling:**
- DO NOT create backup files (.orig, .bak, .swp, etc.)
- DO NOT leave temporary or editor backup files
- Clean up any backup files before finishing
- Only stage actual implementation files

**Git Workflow:**
After implementation is complete and tests pass:
1. Create a git commit using the /commit-commands:commit skill
   - Use a descriptive commit message following conventional commits format
   - Include "Closes #1538" in the commit message
2. Push the changes to origin
3. Create a pull request using the /commit-commands:commit-push-pr skill
   - Link the PR to this issue
   - Include a clear summary of changes and testing done

When you're done, the PR should be created and ready for review.
